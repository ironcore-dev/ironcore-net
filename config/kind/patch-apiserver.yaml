apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: ironcore-net-system
  name: ironcore-net-apiserver
spec:
  template:
    spec:
      containers:
        - name: apiserver
          imagePullPolicy: Never
