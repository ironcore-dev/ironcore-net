---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.0
  name: publicips.apinet.api.onmetal.de
spec:
  group: apinet.api.onmetal.de
  names:
    kind: PublicIP
    listKind: PublicIPList
    plural: publicips
    singular: publicip
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.ipFamily
      name: IPFamily
      type: string
    - jsonPath: .spec.ip
      name: IP
      type: string
    - jsonPath: .status.conditions[?(@.type == "Allocated")].reason
      name: State
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PublicIP is the schema for the publicips API.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              ip:
                description: IP is the ip of the public IP. Pointer to distinguish
                  between unset and explicit zero.
                type: string
              ipFamily:
                description: IPFamily is the ip family of the public ip.
                type: string
            required:
            - ipFamily
            type: object
          status:
            properties:
              conditions:
                description: Conditions are the conditions of a PublicIP.
                items:
                  properties:
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
